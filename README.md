# Система бронювання переговорних кімнат в офісі

Внутрішній веб-застосунок компанії (~60 співробітників, 5 переговорних кімнат на 4–20 місць).
Замінює спільний Google-календар: показує, яка кімната вільна просто зараз, не дає створити дві
броні на той самий час і звільняє кімнату, якщо на зустріч ніхто не прийшов.

Навчальний проєкт, група ІК-43. Базова структура коду за архітектурою з лабораторної роботи №2.

- Крупко Софія — фронтенд (React)
- Приходько Богдана — тімлід, база даних і тестування
- Русанова Вікторія — бекенд (Node.js, Express)

## Що вже працює

Перша ітерація (лаб. №4) закривала вимоги високого пріоритету. Інтеграція (лаб. №5) додала решту
вимог середнього й низького пріоритету — реалізовано і перевірено тестами всі 30 функціональних
вимог специфікації (лаб. №1):

| Можливість                                                   | Вимога                |
| ------------------------------------------------------------ | --------------------- |
| Вхід за корпоративною поштою й паролем                       | ФВ-01, ОБМ-03, НФВ-06 |
| Стан усіх кімнат у поточний момент                           | ФВ-23, НФВ-14         |
| Розклад кімнат на день і на тиждень                          | ФВ-02, ФВ-03          |
| Створення броні з перевірками, серія на 2–8 тижнів           | ФВ-04, ФВ-05, ФВ-24   |
| Заборона накладення броней у часі                            | ФВ-18                 |
| Перевірка місткості кімнати                                  | ФВ-19                 |
| Горизонт бронювання 28 днів                                  | ФВ-27                 |
| Скасування й редагування власної броні                       | ФВ-06, ФВ-07          |
| Скасування чужої броні адміністратором офісу                 | ФВ-10, ФВ-25, ФВ-28   |
| Відмітка про прихід і автоскасування                         | ФВ-08, ФВ-20, НФВ-07  |
| Нагадування за 15 хвилин до початку                          | ФВ-21                 |
| Перелік поточних броней і історія за період                  | ФВ-09, ФВ-11          |
| Вивантаження статистики завантаженості у CSV                 | ФВ-12                 |
| Довідник кімнат, деактивація на період зі скасуванням броней | ФВ-13 – ФВ-15, ФВ-26  |
| Попередження про завелику кімнату                            | ФВ-30                 |
| Облікові записи: створення, блокування                       | ФВ-16, ФВ-17          |
| Журнал подій, зокрема дії самої системи                      | ФВ-22                 |
| Розмежування прав за ролями                                  | ФВ-28, ФВ-29          |
| HTTPS у production (TLS завершує зворотний проксі)           | НФВ-16                |

Нефункціональні вимоги, які перевіряють автотести, а не лише спостереження під час ручного
тестування: точність автоскасування у вікні 10:00–10:30 після початку слота (НФВ-07,
`tests/backend/integration/autoCancelTiming.test.js`, перебирає всі фази тіків планувальника);
відсутність горизонтальної прокрутки від 360 px (НФВ-08, `styles/app.css`); шлях у 3 кліки від
головного екрана до підтвердженої броні (НФВ-01, `tests/frontend/BookingInThreeClicks.test.jsx`);
перевірка здоров'я звертається до бази, а не просто відповідає `200` (НФВ-05, `/api/health`).

## Запуск

Потрібні Node.js 22+ і PostgreSQL 16 (або Docker).

```bash
npm install
cp .env.example .env
docker compose up -d db     # або власний PostgreSQL
npm run db:migrate
npm run db:seed             # 5 кімнат і 4 облікові записи, пароль Passw0rd!
npm run dev                 # API :3000, інтерфейс :5173
```

Без `DATABASE_URL` застосунок стартує зі сховищем у пам'яті — зручно, щоб подивитися інтерфейс,
але дані зникають після перезапуску.

Демо-облікові записи: `olena.kovalenko@example.com` (співробітник),
`iryna.melnyk@example.com` (адміністратор офісу), `taras.shevchuk@example.com` (IT).

Уся система в контейнерах: `docker compose up --build`.

### Команди

| Команда              | Що робить                          |
| -------------------- | ---------------------------------- |
| `npm run dev`        | бекенд і фронтенд одночасно        |
| `npm test`           | усі тести (Vitest)                 |
| `npm run lint`       | ESLint                             |
| `npm run format`     | Prettier                           |
| `npm run build`      | збірка React у `src/frontend/dist` |
| `npm run db:migrate` | застосувати міграції               |
| `npm run db:seed`    | демо-дані                          |

## Структура проєкту

```
src/backend/
  config/          читання змінних середовища в один незмінний об'єкт
  db/              міграції, запуск міграцій, seed, пул з'єднань
  modules/
    auth/          вхід, хешування паролів, JWT, посилання на встановлення пароля
    users/         облікові записи, ролі
    rooms/         довідник кімнат
    bookings/      броні: сутність, правила, сервіс, контролер
      rules/       окреме правило на кожну перевірку (час, горизонт, кімната, місткість)
    eventlog/      журнал подій
    notifications/ листи (SMTP або консоль)
    scheduler/     фонові задачі: автоскасування, нагадування
  shared/          помилки, годинник, робота з датами, CSV, middleware (зокрема вимога HTTPS)
  app.js           складання Express-застосунку з роутерів модулів
  container.js     створення й з'єднання всіх об'єктів (composition root)
  server.js        точка входу

src/frontend/src/
  api/             HTTP-клієнт і виклики REST API (bookingApi.js — усі адреси в одному місці)
  auth/            контекст автентифікації, захист маршрутів
  components/      layout та дрібні елементи форм
  pages/           екрани: вхід, стан кімнат, розклад на день і тиждень, бронювання (зі
                   щотижневою серією й попередженням про завелику кімнату), мої броні, кімнати,
                   користувачі, встановлення пароля
  pages/admin/     розділ адміністратора офісу: поточні броні, історія, статистика, журнал
  styles/          стилі

tests/
  backend/unit/         сутності та правила без бази й мережі
  backend/contract/     спільний набір перевірок для обох репозиторіїв броней
  backend/integration/  сценарії 1–5 через HTTP
  frontend/             компоненти React
  helpers/              тестовий застосунок, фіксований годинник, підміни

docs/                нотатки до структури коду
```

Кожен модуль — окрема папка з власною сутністю, репозиторієм, сервісом, контролером і
маршрутами. Модулі спілкуються через сервіси, а не через чужі репозиторії.

## Як дотримані принципи SOLID

**S.** Сутність `Booking` відповідає за переходи станів броні, `BookingService` — за роботу зі
сховищем і сповіщеннями, `BookingController` — лише за HTTP. Кожне правило бронювання (час,
горизонт, доступність кімнати, місткість) — окремий клас з однією причиною для змін.

**O.** Нову перевірку при бронюванні додають новим класом-нащадком `BookingRule` і одним рядком у
`defaultBookingRules()`; `BookingService` при цьому не змінюється. Так само додають задачу
планувальника або канал сповіщень.

**L.** `InMemoryBookingRepository` і `PgBookingRepository` взаємозамінні: обидва проходять той
самий набір перевірок `tests/backend/contract/bookingRepositoryContract.js`. Тому тести бізнес-логіки
працюють зі сховищем у пам'яті, а поведінка лишається тією самою, що й з PostgreSQL.

**I.** Інтерфейси вузькі: `PasswordHasher` — це `hash`/`verify`, `NotificationChannel` — один метод
`send`, `Clock` — один метод `now`. Тестові підміни реалізують рівно ці методи.

**D.** `BookingService` залежить від абстракцій `BookingRepository`, `NotificationChannel`, `Clock`,
а не від `pg`, `nodemailer` чи системного часу. Конкретні реалізації обирає `container.js`, тому
заміна бази чи поштового сервера не зачіпає бізнес-логіку.

## Заборона накладення броней

Головна вимога замовника (ФВ-18) тримається на обмеженні рівня бази даних, а не лише на коді:

```sql
CONSTRAINT bookings_no_overlap EXCLUDE USING gist (
    room_id WITH =,
    tstzrange(start_time, end_time, '[)') WITH &&
) WHERE (status IN ('ACTIVE', 'CONFIRMED'))
```

Навіть якщо два запити надійдуть одночасно, PostgreSQL прийме рівно один, а другий отримає помилку
`23P01`, яку `PgBookingRepository` перетворює на `SlotTakenError` і відповідь `409 Conflict`.
Сценарій 2 зі специфікації вимог перевіряють тести контракту й інтеграційний тест.

## Лабораторна робота №5: інтеграція та управління залежностями

Метод інтеграції — локальний: один репозиторій, один Docker-образ застосунку (лаб. №2,
діаграма розгортання), інтеграційні тести звертаються до застосунку через REST API на
`supertest`, а не до розгорнутого в хмарі середовища. Це узгоджується з монолітною
архітектурою й розміром команди, обраними в лабораторній роботі №2.

**Управління залежностями.** Один `package.json` у корені з npm workspaces (`src/backend`,
`src/frontend`); `package-lock.json` фіксує точні версії для відтворюваної збірки. Нових
production-залежностей не додавали — весь код лаб. №5 написано на вже узгодженому стеку
(Express, `pg`, React). `npm ci` в CI та в `Dockerfile` встановлює рівно ті версії, що
зафіксовані в lock-файлі, тому збірка на CI, у Docker і на машині розробниці не розходиться.

**Що перевірено при інтеграції:**

- усі модулі бекенду (Bookings, Rooms, Users, EventLog, Notifications, Scheduler) зібрані в
  один Express-застосунок і використовують один пул з'єднань PostgreSQL (`container.js`);
- `RoomService` і `BookingService` з'єднані через слухача деактивації
  (`roomService.addDeactivationListener(bookingService)`) — модуль Rooms не залежить від
  Bookings напряму, залежність лише в одну сторону, як і описано в компонентній діаграмі;
- REST API між React SPA і бекендом пройдено наскрізно: `npm run build` збирає фронтенд,
  Express роздає його статично і повертає `index.html` на будь-який інший шлях (SPA-роутинг
  одним сервером, лаб. №2);
- на реальному PostgreSQL 16 (не лише в тестовому оточенні) прогнано міграції
  (`npm run db:migrate`), сід демо-даних (`npm run db:seed`) і запущено сервер — уручну
  перевірено кожен новий маршрут: створення й редагування броні з накладенням і перевищенням
  місткості, серію з пропущеним тижнем, деактивацію кімнати з каскадним скасуванням і листом,
  перелік і історію броней адміністратора, вивантаження CSV, журнал подій, а також усі три
  сценарії HTTPS (перенаправлення, відмову без TLS, прохід через `X-Forwarded-Proto`);
- повний набір автотестів (204 тести: unit, contract для обох реалізацій репозиторію броней,
  інтеграційні через HTTP, компонентні на React) пройдено на тому самому образі PostgreSQL 16,
  що й у `docker-compose.yml` та CI — тестове середовище не розходиться з production;
- `npm run lint`, `npx prettier --check .` і `npm run build` пройдено без попереджень.

**Відповідність вимогам.** Таблиця в розділі «Що вже працює» зіставляє кожну функціональну
вимогу лаб. №1 з реалізованою можливістю; для НФВ, які піддаються автоматичній перевірці, там
само вказано конкретний тест. Решта НФВ (доступність 99,5 %, резервне копіювання щодня,
відновлення за 2 години, підтримка 4 браузерів) — експлуатаційні характеристики, які
підтверджуються процесом розгортання й моніторингом, а не одним тестовим прогоном, і
перевірятимуться після виходу в продакшн.

## Робота з Git

- `main` — захищена гілка, у неї потрапляє лише злитий pull request;
- гілки формату `feature/<модуль>-<задача>`, наприклад `feature/bookings-core`;
- одна задача з дошки Trello — одна гілка й один pull request;
- перед злиттям — код-рев'ю іншою учасницею команди та зелений CI
  (ESLint, Prettier, тести, збірка фронтенду).
